const deviceSizes = {
  mobile: '414px',
};

const device = {
  mobile: `only screen and (max-width: ${deviceSizes.mobile})`,
};

const colors = {
  main: '#ff5252',
};

export const theme = {
  device,
  colors,
  deviceSizes,
};
